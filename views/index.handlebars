<div>{{burger_name}}</div>
<div>{{devoured}}</div>


<div>
    {{#each burger_data}} {{#unless this.devoured}}
    <div class="card">
        <div class="card-body">
            {{this.id}}, {{this.burger_name}}
            <button type="button" class="btn btn-warning float-right">Light</button>
        </div>
    </div>
    {{/unless}} {{/each}}
</div>



<body>
    <img src="../public/assets/img/burgerman.jpg" class="rounded mx-auto my-3 d-block" alt="burger man">

    <div class="container">
        <div class="row justify-content-around">
            <div class="col-4">
                <div>
                    {{#each burger_data}} {{#unless this.devoured}}
                    <div class="card">
                        <div class="card-body">
                            {{this.id}}. {{this.burger_name}}
                            <button type="button" class="btn btn-warning float-right" id="devourButton">Light</button>
                        </div>
                    </div>
                    {{/unless}} {{/each}}
                </div>
            </div>
            <div class="col-4">
                <div>
                    {{#each burger_data}} {{#if this.devoured}}
                    <div class="card">
                        <div class="card-body">
                            {{this.id}}. {{this.burger_name}}
                        </div>
                    </div>
                    {{/if}} {{/each}}
                </div>
            </div>
        </div>
    </div>

</body>